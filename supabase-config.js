/* ============================================================
   ATEJ · Painel de Gestão — configuração do Supabase
   Preencha com os dados do SEU projeto (Supabase → Project Settings → API)
   A "anon public key" é segura para ficar aqui — ela é pública por
   natureza, o controle de acesso real é feito pelas regras (RLS)
   e pela Edge Function no servidor. NUNCA coloque a "service_role"
   key neste arquivo.
   ============================================================ */
const SUPABASE_URL = 'https://SEU-PROJETO.supabase.co';
const SUPABASE_ANON_KEY = 'SUA-CHAVE-ANON-PUBLICA-AQUI';
